generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model posts {
  id    String @id @default(uuid())
  name  String
  email String @unique
  date  String @unique
}
